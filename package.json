{
  "name": "avaliador-scti",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc",
    "deploy": "npm run compile && sls deploy -s dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.85",
    "@types/node": "^16.11.6",
    "serverless-offline": "^8.2.0",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "aws-lambda": "^1.0.6",
    "dotenv": "^10.0.0"
  }
}
